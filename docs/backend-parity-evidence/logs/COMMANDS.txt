# Commands executed (verification stage)
cd C:\candle-build\candle
set CARGO_TARGET_DIR=C:\candle-build\target
set CANDLE_REQUIRE_CUDA_TEST_DEVICE=1
set CANDLE_REQUIRE_VULKAN_TEST_DEVICE=1
set CANDLE_REQUIRE_WGPU_TEST_DEVICE=1
set CANDLE_EXPECTED_GPU_NAME=RTX 3060
set CANDLE_STRICT_NO_CPU_FALLBACK=1
set CANDLE_NUMERICAL_REPORT=docs/backend-parity-evidence/numerical/diff_report.json

cargo test -p candle-core --features "cuda,vulkan,wgpu" --test gpu_parity_matrix_tests -- --nocapture
cargo test -p candle-core --features "cuda,vulkan,wgpu" --test gpu_numerical_diff_tests -- --nocapture
cargo run -p candle-core --release --features "cuda,vulkan,wgpu" --example fallback_runtime_audit
cargo run -p candle-core --release --features "cuda,vulkan,wgpu" --example backend_parity_microbench -- --suite
